import Image from "next/image";import {Apple} from "lucide-react";import {useLocale,useTranslations} from "next-intl";import type {Locale} from "../../i18n";import {appStoreUrl} from "./app-store";import Reveal from "./Reveal";
export default function DownloadV2(){const t=useTranslations("v2"),locale=useLocale() as Locale;return <section id="download" className="v2-section py-24"><div className="v2-container"><Reveal><div className="download-panel"><div><h2>{t("download.h2")}</h2><p>{t("download.sub")}</p><div className="mt-8 flex flex-wrap items-center gap-3"><a href={appStoreUrl(locale)} target="_blank" rel="noreferrer" className="app-store-badge large"><Apple fill="currentColor" size={30}/><span><small>Download on the</small><strong>App Store</strong></span></a><span className="soon-pill">{t("playSoon")}</span></div></div><div className="qr-card hidden md:block"><Image src={`/qr-codes/app-store-${locale}.png`} width={154} height={154} alt={t("download.qr")}/><span>{t("download.qr")}</span></div></div></Reveal></div></section>}

